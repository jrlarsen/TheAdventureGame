(function () {
    "use strict";

    TAG.Views = TAG.Views || {};
    TAG.Views.info = {

        log: function (message) {
            console.log("*** " + message + " ***");
        }

    };

})();